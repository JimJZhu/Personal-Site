{
    "name": "personal-site",
    "version": "1.1.0",
    "description": "My personal site",
    "main": "index.js",
    "dependencies": {
        "bower": "^1.8.0"
    },
    "devDependencies": {
        "concurrently": "^3.4.0",
        "jshint": "^2.9.4",
        "lite-server": "^2.3.0",
        "mocha": "^3.4.2",
        "pug-cli": "^1.0.0-alpha6",
        "stylus": "^0.54.5",
        "uglify-js": "^3.0.18"
    },
    "scripts": {
        "dev-setup": "lite-server -c bs-config.json",
        "dev": "lite-server",
        "copy": "cp -rf src/assets dist && cp -rf src/assets dist && cp -rf src/fonts dist && cp -f src/css/font-awesome.min.css dist/css",
        "build-js": "mkdir -p dist/html && mkdir -p dist/js && mkdir -p dist/css && pug src/index.pug src/bonus.pug -o dist/ && uglifyjs src/js/app.js -m -o dist/js/app.min.js && uglifyjs src/js/ui.js -m -o dist/js/ui.min.js && stylus src/css/loader.styl -o dist/css/stylesheet.css && npm run copy",
        "clean": "rm -R dist",
        "clean-build": "npm run clean && npm run build-js",
        "lint": "jshint src/**/**.js",
        "watch": "concurrently 'stylus -w src/css/loader.styl -o dist/css/stylesheet.css' 'pug -w src/index.pug -o dist/' ",
        "test": "mocha test",
        "pretest": "npm run lint",
        "posttest": "echo the test has been run!",
        "deploy": "dploy"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/JimJZhu/Personal-Site.git"
    },
    "author": "Jim J. Zhu",
    "license": "ISC",
    "bugs": {
        "url": "https://github.com/JimJZhu/Personal-Site/issues"
    },
    "homepage": "https://github.com/JimJZhu/Personal-Site#readme"
}